spring.datasource.url=${DB_URL:jdbc:mysql://127.0.0.1:3306/quiz_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:eyeseemysql11}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

spring.sql.init.mode=never

server.port=${SERVER_PORT:8080}
server.servlet.session.timeout=30m

# Disable static resource mappings (if using frontend separately)
spring.web.resources.add-mappings=false

app.jwt.secret=${APP_JWT_SECRET:my-super-secret-jwt-key-for-quiz-platform-2024-secure-key-very-long-and-secure-key-for-production-use-with-256-bits-minimum}
app.jwt.ttlSeconds=${APP_JWT_TTL_SECONDS:86400}
app.jwt.refreshTtlSeconds=${APP_JWT_REFRESH_TTL_SECONDS:604800}


spring.application.name=quiz-backend


gemini.api.key=${GEMINI_API_KEY:AIzaSyBO4wKZ_bpgEn4d8GyGhWeMDISwmHE1E40}
gemini.api.url=${GEMINI_API_URL:https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent}
